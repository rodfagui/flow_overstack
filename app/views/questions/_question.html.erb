<li id="question-<%= question.id %>">
  <h1 class="title"><%= link_to question.title, question %></h1>
  <span class="questioner"> Asked by <%= link_to question.user.email, question.user %> <%= time_ago_in_words(question.created_at) %> ago. </span>
</li>